<UserControl x:Class="ModAssetMapper.Views.AssetListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ModAssetMapper.Views"
             mc:Ignorable="d"
             DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=AssetListViewModel}">
    <StackPanel Orientation="Vertical">
        <Label Content="Assets" Style="{StaticResource DescriptionHeaderStyle}" />
        <ListBox ItemsSource="{Binding Assets, Mode=OneWay}" BorderBrush="{DynamicResource AccentColorBrush}"
                 BorderThickness="0 1 0 0"/>
    </StackPanel>
</UserControl>
